#include <iostream>
#include <vector>
#include <string>
#include <st_node.hpp>
using namespace std;

opcode semantic_analyser::set_st_root(prog*)
{
	vector<node*> worklist
	if(!prog){
		size_t last_size = 0;
		size_t i = 0;
		worklist.push_back(prog);
		while(worklist.size()!=last_size){
			last_size = worklist.size();
			worklist[i]
		}
	}
}
bool semantic_analyser::sem_check()
{

}

semantic_analyser::semantic_analyser()
{

}
semantic_analyser::~semantic_analyser()
{

}

